import 'package:flutter/material.dart';

/// Shared palette for the "liquid glass" look — soft pastel surfaces with
/// an indigo/pink accent gradient used across onboarding and CTAs.
abstract final class AppColors {
  static const backgroundTop = Color(0xFFDCEFEA);
  static const backgroundMid = Color(0xFFE9E2F6);
  static const backgroundBottom = Color(0xFFF6DCEE);

  static const accentStart = Color(0xFF6E63E0);
  static const accentEnd = Color(0xFFE07BC0);

  static const textPrimary = Color(0xFF2C2A4A);
  static const textSecondary = Color(0xFF6E6A85);

  static const glassBorder = Color(0xFFFFFFFF);
  static const dotInactive = Color(0xFFCFC9DE);
}